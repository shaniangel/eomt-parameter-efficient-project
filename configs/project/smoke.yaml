# Quick end-to-end check, stack last: ... -c configs/project/<regime>.yaml -c configs/project/smoke.yaml
trainer:
  max_epochs: 1
  limit_train_batches: 20
  limit_val_batches: 5
  num_sanity_val_steps: 0
  log_every_n_steps: 1
